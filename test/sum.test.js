const { assert, expect, should } = require('chai');
const { describe } = require('mocha')

const sum = require('../src/sum.js');

describe('#sum()', function() {

  beforeEach(function() {});

  context('default', function(){

    it('can parse a sum command', function() {
    });

  });

});